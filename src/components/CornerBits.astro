---

---

<div class="CornerBits">
	<div class="bit topLeft"></div>
	<div class="bit topRight"></div>

	<div class="bit bottomLeft"></div>
	<div class="bit bottomRight"></div>
</div>


<style lang="scss">
	.CornerBits{
		position: absolute;
		width: 100%;
		height: 100%;
		pointer-events: none;
	}
	.bit{
		width: 50px;
		height: 50px;
		position: absolute;
	}
	.topLeft{
		top: var(--padding-1);
		left: var(--padding-1);
		border-top: solid var(--border-width-2) var(--border-color);
		border-left: solid var(--border-width-2) var(--border-color);
	}
	.bottomRight{
		top: var(--padding-1);
		right: var(--padding-1);
		border-top: solid var(--border-width-2) var(--border-color);
		border-right: solid var(--border-width-2) var(--border-color);
	}
	.bottomLeft{
		bottom: var(--padding-1);
		left: var(--padding-1);
		border-bottom: solid var(--border-width-2) var(--border-color);
		border-left: solid var(--border-width-2) var(--border-color);
	}
	.topRight{
		bottom: var(--padding-1);
		right: var(--padding-1);
		border-bottom: solid var(--border-width-2) var(--border-color);
		border-right: solid var(--border-width-2) var(--border-color);
	}
</style>